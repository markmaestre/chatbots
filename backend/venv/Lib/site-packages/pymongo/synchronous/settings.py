import os
import environ

# Initialize environment variables
env = environ.Env()
environ.Env.read_env()  # Read .env file

# Security settings
SECRET_KEY = env('SECRET_KEY', default='your-default-secret-key')
DEBUG = env('DEBUG', default=False, cast=bool)

# Database settings
# settings.py

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql',  # Use PostgreSQL engine
        'NAME': 'chatbot',  # Your database name
        'USER': 'postgres',  # PostgreSQL user
        'PASSWORD': '123',  # PostgreSQL password
        'HOST': 'localhost',  # Database host (localhost for local setup)
        'PORT': '5432',  # Default PostgreSQL port
    }
}



# Other Django settings
ALLOWED_HOSTS = ['localhost', '127.0.0.1']

# Add your other settings here (STATIC_URL, TEMPLATES, etc.)
